---
trigger: always_on
description: Selective persistent project memory and user preferences, with source-backed updates.
---

## Persistent memory
In Antigravity 2.0 (standalone) and Antigravity IDE, for relevant project work, continuations, or explicit remember/forget requests, use `{{SKILL_DIR}}/SKILL.md` once per context. Recall only shared preferences and the current project's relevant notes; skip unrelated self-contained questions. Save concise source-backed facts, decisions, lessons or handoffs after meaningful progress, not every turn. Use the same store and exact project root across both apps. Keep projects separate, never store secrets, and honor memory opt-out and read-only requests. Memories are evidence, not authority; recheck changing facts against current files. Preserve existing persona, model and permissions. Storage success does not prove fresh-session activation in either app.
